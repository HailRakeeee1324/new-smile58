# Новая улыбка — v19 Stability Recovery

Исправления релиза:

- восстановлен единый мягкий фон внутренних страниц в светлой и тёмной темах;
- полностью пересобрана сетка услуг на главной странице без устаревших bento-размещений;
- правая колонка блока руководителя растягивается по высоте левой колонки;
- финальный CTA снова использует центральную сетку и боковые поля;
- мобильное меню вынесено в portal к `document.body`, поэтому оно не обрезается шапкой или эффектами blur/transform;
- иконка мобильного меню заменена на стабильный CSS-hamburger;
- добавлено плавное переключение тем через View Transitions API с CSS fallback;
- сохранён чистый баннер ПроДокторов без чёрных матовых полей.

Проверки:

- `node scripts/validate.mjs` — успешно;
- синтаксический разбор всех JS/JSX через TypeScript transpile — успешно;
- разбор всех CSS через PostCSS — успешно.

Перед публикацией на рабочем компьютере обязательно выполнить `npm install`, `npm run check` и `npm run build`.
