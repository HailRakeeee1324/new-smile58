# Исправление карточек филиалов на главной

Что исправлено:
- На desktop-версии карточки филиалов на главной больше не накладывают номер телефона на адрес.
- Адресная часть карточки остаётся кликабельной и ведёт на страницу филиалов с нужным филиалом.
- Номер телефона остаётся отдельной нативной `tel:`-ссылкой для прямого звонка.
- Мобильная логика не переписывалась, правка ограничена desktop-режимом от 921px.

Проверка:
- `npm run build` проходит успешно.
- Prerender генерирует 40 страниц.
